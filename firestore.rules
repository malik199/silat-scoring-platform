rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all access by default until auth rules are implemented.
    // Auth rules will be added when the authentication flow is scaffolded.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
