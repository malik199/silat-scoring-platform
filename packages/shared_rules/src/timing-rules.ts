/** Total number of rounds per match */
export const ROUND_COUNT = 3;

/** Duration of a single round in seconds */
export const ROUND_DURATION_SECONDS = 120; // 2 minutes

/** Duration of the break between rounds in seconds */
export const BREAK_DURATION_SECONDS = 60; // 1 minute

/** Number of judges per arena */
export const JUDGE_COUNT = 3;
